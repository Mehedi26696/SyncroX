# Project dependencies
# -------- Frontend (Streamlit UI) --------
streamlit==1.36.0

# -------- Backend HTTP API --------
fastapi==0.111.0
uvicorn==0.30.1

# -------- Code Execution / Sandbox --------
docker==7.0.0          # Only if you plan to run code inside Docker containers

# -------- Utility & Common --------
python-multipart==0.0.9   # Required for file uploads in FastAPI
requests==2.31.0
pydantic==2.10.5

# -------- Optional (for logs, CORS, hashing etc.) --------
python-dotenv==1.0.1
passlib==1.7.4
streamlit-autorefresh==1.0.1

